/** httpOnly auth cookies set by the API (must match apps/api/src/auth/auth-cookies.ts). */
export const AUTH_ACCESS_COOKIE = "nexsmsid.accessToken";
export const AUTH_REFRESH_COOKIE = "nexsmsid.refreshToken";
