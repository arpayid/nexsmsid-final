# HTTP-only mode (no DOMAIN set) — serves the app directly over port 80.
# Used for IP/localhost pilots and staging without TLS.
server {
    listen 80 default_server;
    server_name _;

    client_max_body_size 25M;

    include /etc/nginx/snippets/proxy_locations.conf;
}
